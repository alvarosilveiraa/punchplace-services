'use strict';

module.exports = app => {
  // app.validator.addRule('nullString', (rule, value) => {
  //   if(rule.required && (!value || typeof value !== 'string'))
  //     return 'should be a string';
  //   else if(value !== null && typeof value !== 'string')
  //     return 'should be a string or null';
  // });
}
